@{
    ViewData["Title"] = "Home Page";
}
@using System.Globalization
@model IQueryable<Book>

@foreach (var b in Model)
{
    <div>
        <h3>@b.Title</h3>
        <h4>@b.Author?.Name</h4>
        @b.Description
        <h4>@b.Price.ToString("c2", new CultureInfo("ru-RU"))</h4>
    </div>
}

